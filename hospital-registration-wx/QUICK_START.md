# 🚀 微信小程序快速开始

## ✅ 已完成的配置

我已经为你创建了以下核心文件：

### 1️⃣ 配置文件（已创建）
- ✅ `package.json` - 项目配置
- ✅ `manifest.json` - 应用清单
- ✅ `pages.json` - 页面配置
- ✅ `App.vue` - 应用入口
- ✅ `main.js` - 主入口文件
- ✅ `config/app.js` - 应用配置

### 2️⃣ API文件（已创建）
- ✅ `utils/request.js` - 网络请求封装
- ✅ `api/user.js` - 用户相关API
- ✅ `api/api.js` - 业务相关API

### 3️⃣ 页面文件（已创建示例）
- ✅ `pages/index/index.vue` - 首页（完整示例）
- ✅ `pages/my/my.vue` - 我的页面（完整示例）

---

## 🔥 立即开始运行

### 方法一：HBuilderX（最简单，推荐）

#### 1. 安装HBuilderX
下载地址：https://www.dcloud.io/hbuilderx.html

#### 2. 打开项目
```
HBuilderX → 文件 → 打开目录
选择：D:\github-project\hospitalRegistePublic\hospital-registration-wx
```

#### 3. 运行
```
顶部菜单 → 运行 → 运行到小程序模拟器 → 微信开发者工具
```

**注意**：首次运行需要配置微信开发者工具路径

---

### 方法二：微信开发者工具直接导入

#### 1. 安装微信开发者工具
下载地址：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

#### 2. 打开项目
```
微信开发者工具 → 导入项目
项目目录：D:\github-project\hospitalRegistePublic\hospital-registration-wx
AppID：选择"测试号"
```

#### 3. 编译运行
点击"编译"按钮即可

---

## 📱 当前功能状态

### ✅ 可用功能
1. **首页** - 显示科室列表和功能菜单
2. **我的** - 用户中心页面
3. **API接口** - 完整的后端接口调用

### 🔨 需要补充的页面
以下页面文件是空的，需要继续开发：

- `pages/schedulingRecord/schedulingRecord.vue` - 排班列表
- `pages/subscribeInfo/subscribeInfo.vue` - 预约信息
- `pages/registrationRecord/registrationRecord.vue` - 挂号记录
- `pages/registrationDetail/registrationDetail.vue` - 挂号详情
- `pages/myVisitors/myVisitors.vue` - 就诊人管理
- `pages/payRecord/payRecord.vue` - 充值记录
- `pages/orderPayStatus/orderPayStatus.vue` - 支付状态
- `pages/nucleicAcidPayStatus/nucleicAcidPayStatus.vue` - 核酸支付状态
- `pages/user/info/info.vue` - 个人信息

---

## 🔧 快速开发指南

### 创建新页面的步骤：

1. **复制现有页面作为模板**
```bash
# 以首页为模板
复制 pages/index/index.vue → pages/新页面/新页面.vue
```

2. **修改页面内容**
- 修改 `<template>` 中的HTML结构
- 修改 `<script>` 中的数据和方法
- 修改 `<style>` 中的样式

3. **在pages.json中注册**
已在 `pages.json` 中注册了所有页面路由

4. **调用API接口**
```javascript
import { getSectionList } from '@/api/api.js'

async loadData() {
  const res = await getSectionList({
    pageNum: 1,
    pageSize: 10
  })
  this.list = res.rows
}
```

---

## 🎯 开发优先级建议

### 第1优先级（核心功能）
1. ✅ 首页 - 已完成
2. ✅ 我的 - 已完成  
3. 📝 排班列表 - 待开发
4. 📝 预约挂号 - 待开发
5. 📝 挂号记录 - 待开发

### 第2优先级（辅助功能）
6. 📝 就诊人管理 - 待开发
7. 📝 充值记录 - 待开发
8. 📝 个人信息 - 待开发

### 第3优先级（扩展功能）
9. 📝 支付功能 - 待开发
10. 📝 评价功能 - 待开发

---

## 🌐 后端API配置

### 修改API地址
编辑 `config/app.js`：

```javascript
export default {
  // 本地开发
  baseUrl: 'http://localhost:8080',
  
  // 或使用局域网IP（手机真机测试）
  // baseUrl: 'http://192.168.1.100:8080',
  
  // 生产环境
  // baseUrl: 'https://your-domain.com/api',
}
```

**注意**：
- 微信小程序要求API必须是HTTPS（正式版）
- 开发时可以在微信开发者工具中关闭"不校验合法域名"

---

## 📋 页面开发模板

### 列表页面模板
```vue
<template>
  <view class="container">
    <view class="list-item" v-for="item in list" :key="item.id">
      <text>{{ item.name }}</text>
    </view>
  </view>
</template>

<script>
import { getDataList } from '@/api/api.js'

export default {
  data() {
    return {
      list: [],
      loading: false
    }
  },
  onLoad() {
    this.loadData()
  },
  methods: {
    async loadData() {
      try {
        this.loading = true
        const res = await getDataList({
          pageNum: 1,
          pageSize: 10
        })
        this.list = res.rows
      } catch (error) {
        console.error('加载失败', error)
      } finally {
        this.loading = false
      }
    }
  }
}
</script>

<style lang="scss" scoped>
.container {
  padding: 20rpx;
}
.list-item {
  padding: 30rpx;
  background: #fff;
  margin-bottom: 20rpx;
  border-radius: 12rpx;
}
</style>
```

---

## 🎨 UI组件库

项目已集成 **uView UI**，可以直接使用：

```vue
<!-- 按钮 -->
<u-button type="primary">按钮</u-button>

<!-- 输入框 -->
<u-input v-model="value" placeholder="请输入"></u-input>

<!-- 加载中 -->
<u-loading></u-loading>

<!-- 更多组件查看文档 -->
```

文档：https://www.uviewui.com/

---

## 📱 调试技巧

### 1. 查看控制台
微信开发者工具 → 控制台 → 查看日志

### 2. 查看网络请求
微信开发者工具 → Network → 查看API调用

### 3. 真机调试
点击"预览" → 扫描二维码 → 手机微信打开

### 4. 查看存储
控制台 → Storage → 查看缓存数据

---

## ❓ 常见问题

### Q1: 页面显示空白？
A: 检查是否正确导入了组件和API

### Q2: API请求失败？
A: 
1. 检查后端是否启动（http://localhost:8080）
2. 检查 `config/app.js` 中的baseUrl配置
3. 在微信开发者工具中关闭"不校验合法域名"

### Q3: 如何使用真机测试？
A: 
1. 电脑和手机连接同一WiFi
2. 修改 `config/app.js` 的baseUrl为局域网IP
3. 点击"预览"生成二维码，用微信扫码

### Q4: 如何发布小程序？
A: 
1. 点击"上传"按钮
2. 登录微信公众平台
3. 提交审核
4. 审核通过后发布

---

## 📞 需要帮助？

### 完整文档
查看 `WX_MINIPROGRAM_GUIDE.md` 获取详细说明

### 开发建议
1. 先确保后端API正常运行
2. 使用HBuilderX或微信开发者工具打开项目
3. 参考已完成的首页和我的页面
4. 逐个完成其他页面

### 项目架构
```
hospital-registration-wx/
├── config/         # 配置文件
├── api/            # API接口
├── utils/          # 工具函数
├── pages/          # 页面文件
├── components/     # 组件
├── static/         # 静态资源
└── uni_modules/    # uni-app插件
```

---

## ✨ 快速总结

1. **已完成**：配置文件、API接口、首页、我的页面
2. **可以运行**：使用HBuilderX或微信开发者工具打开即可
3. **需要继续**：补充其他业务页面（约9个页面）
4. **开发时间**：参考现有页面，每个页面约1-2小时

**建议**：先确保Web前端和后端完全稳定，再开发小程序！


