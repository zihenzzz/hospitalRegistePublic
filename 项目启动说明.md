# 医院预约挂号系统 - 项目启动说明

## ✅ 已完成的工作

### 1. 数据库相关
- ✅ 完整的SQL建表脚本：`hospital_registration.sql`
- ✅ 包含22张表（15张系统表 + 7张业务表）
- ✅ 初始化数据（管理员账号、菜单、字典等）

### 2. 后端配置文件
- ✅ `application.yml` - 主配置文件
- ✅ `application-druid.yml` - 数据源配置文件

### 3. 业务代码
- ✅ PSection（科室管理）- 实体类 + Mapper XML
- ✅ POutpatien（门诊管理）- 实体类 + Mapper XML
- ✅ PScheduling（排班管理）- 实体类 + Mapper XML
- ✅ PRegistration（挂号管理）- 实体类 + Mapper XML
- ✅ PPay（充值记录）- 实体类 + Mapper XML
- ✅ PCollect（收藏管理）- 实体类 + Mapper XML
- ✅ PEvaluate（评价管理）- 实体类 + Mapper XML

## 🚀 启动步骤

### 一、环境准备

确保已安装以下软件：
- ✔️ JDK 1.8
- ✔️ MySQL 5.7+
- ✔️ Redis 5.0+
- ✔️ Node.js 14.21+
- ✔️ Maven 3.6+
- ✔️ IDEA/Eclipse（后端开发工具）
- ✔️ VSCode（前端开发工具）

### 二、数据库配置

#### 1. 创建数据库并导入SQL
```bash
# 方式1：使用Navicat
打开Navicat → 连接MySQL → 运行 hospital_registration.sql

# 方式2：使用命令行
mysql -u root -p
source D:/github-project/hospitalRegistePublic/hospital_registration.sql
```

#### 2. 验证数据库
- 数据库名：`hospital_registration`
- 默认管理员：admin / 123456

### 三、启动Redis

```bash
# Windows
redis-server.exe

# Linux/Mac
redis-server
```

### 四、配置后端

#### 1. 修改配置文件
打开 `hospital-registration-back-end/pipilin-admin/src/main/resources/application-druid.yml`

```yaml
# 修改MySQL密码（必须）
spring:
  datasource:
    druid:
      master:
        password: 你的MySQL密码  # 改这里
```

#### 2. 修改Redis配置（如果有密码）
打开 `hospital-registration-back-end/pipilin-admin/src/main/resources/application.yml`

```yaml
spring:
  redis:
    password:   # 如果Redis设置了密码，在这里填写
```

#### 3. 修改文件上传路径（可选）
在 `application.yml` 中：
```yaml
pipilin:
  # Windows系统
  profile: D:/pipilin/uploadPath
  # Linux系统
  # profile: /home/pipilin/uploadPath
```

### 五、启动后端项目

#### 方式1：使用IDEA
1. 用IDEA打开 `hospital-registration-back-end` 文件夹
2. 等待Maven依赖下载完成
3. 找到 `pipilin-admin/src/main/java/com/pipilin/PipilinApplication.java`
4. 右键 → Run 'PipilinApplication'
5. 看到 "PipilinApplication start success" 表示启动成功

#### 方式2：使用命令行
```bash
cd hospital-registration-back-end
mvn clean install
cd pipilin-admin
mvn spring-boot:run
```

#### 验证后端启动
浏览器访问：
- 后台管理：http://localhost:8080
- Druid监控：http://localhost:8080/druid （admin/123456）
- Swagger文档：http://localhost:8080/swagger-ui/

### 六、启动前端项目

#### 1. 安装依赖
```bash
cd hospital-registration-front-end
npm install
```

#### 2. 启动开发服务器
```bash
npm run dev
```

#### 3. 访问前端
浏览器会自动打开，或手动访问：http://localhost:80

#### 默认登录账号
- 用户名：`admin`
- 密码：`123456`

### 七、启动小程序（可选）

#### 1. 修改配置
打开 `hospital-registration-wx/api/api.js`，修改后端接口地址：
```javascript
const baseUrl = 'http://localhost:8080'  // 改成你的后端地址
```

#### 2. 配置微信小程序
打开 `hospital-registration-wx/manifest.json`
```json
{
  "mp-weixin": {
    "appid": "你的小程序AppId"  // 改这里
  }
}
```

#### 3. 运行小程序
使用HBuilder打开 `hospital-registration-wx` 项目
- 点击 运行 → 运行到小程序模拟器 → 微信开发者工具
- 或者：运行 → 运行到浏览器

## 📋 功能模块

### 管理后台功能
- ✅ 用户管理（管理员、医生、患者）
- ✅ 角色权限管理
- ✅ 菜单管理
- ✅ 科室管理
- ✅ 门诊管理
- ✅ 医生排班管理
- ✅ 挂号信息管理
- ✅ 充值记录管理
- ✅ 收藏信息管理
- ✅ 评价信息管理
- ✅ 通知公告管理
- ✅ 系统日志
- ✅ 数据字典

### 小程序功能
- ✅ 预约挂号
- ✅ 就诊充值
- ✅ 充值记录
- ✅ 医生排班查询
- ✅ 挂号记录
- ✅ 就诊人信息管理
- ✅ 医生收藏
- ✅ 医生评价

## 🐛 常见问题

### 1. 后端启动失败
**问题：** 连接数据库失败
```
解决：检查 application-druid.yml 中的数据库配置
- 确认MySQL已启动
- 确认用户名密码正确
- 确认数据库 hospital_registration 已创建
```

**问题：** 连接Redis失败
```
解决：
- 确认Redis已启动
- 检查Redis端口是否为6379
- 如有密码，在application.yml中配置
```

### 2. 前端启动失败
**问题：** npm install 失败
```bash
解决：
# 清除缓存
npm cache clean --force
# 使用淘宝镜像
npm install --registry=https://registry.npmmirror.com
```

**问题：** 页面空白或无法访问
```
解决：
- 确认后端已启动
- 检查浏览器控制台错误信息
- 确认端口未被占用
```

### 3. 登录失败
**问题：** 用户名或密码错误
```
解决：
- 默认账号：admin
- 默认密码：123456
- 如果还是不行，检查数据库sys_user表是否有数据
```

## 📞 技术栈

### 后端
- Spring Boot 2.5.14
- Spring Security
- MyBatis
- MySQL 5.7+
- Redis 5.0+
- Druid连接池
- JWT认证

### 前端
- Vue 2.x
- Element UI
- Axios
- Vue Router

### 小程序
- uni-app
- uView UI
- 微信小程序

## 🎉 项目现在可以运行了！

所有代码已经生成完毕，按照上述步骤操作即可启动项目。

祝你使用愉快！ 🚀



